• DynEQ UI: Add Attack/Release/Hold controls (sliders + labels) and bind to per-band params.
• DynEQ UI audit: Review full UI capabilities against backend (bands, modes, SC, lookahead) and plan coverage.
• DynEQ visualization: Add clear attenuation/GR visual (per-band GR handle/path or meter overlay).

